import {SessionHeaderCard} from "./card"
import {SessionHeaderHandler} from "./view"

let sessionHeaderAnalysis = {
    card: SessionHeaderCard,
    viewHandler: SessionHeaderHandler,
    route: 'header'
}

export default sessionHeaderAnalysis
