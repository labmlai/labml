import {DiskCard} from "./card"
import {DiskHandler} from "./view"
import {Analysis} from "../../types"

let diskAnalysis: Analysis = {
    card: DiskCard,
    viewHandler: DiskHandler,
    route: 'disk'
}

export default diskAnalysis
